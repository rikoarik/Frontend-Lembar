import { render,screen } from '@testing-library/react';import { describe,it,expect,vi } from 'vitest';import AssessmentResults from '../AssessmentResults';
vi.stubGlobal('fetch',vi.fn().mockResolvedValue({ok:true,json:async()=>({data:[{id:'a1',guestName:'Budi',guestClass:'7A',status:'submitted',score:8,maxScore:10,needsGrading:true,submittedAt:'2026-08-09T00:00:00Z'}]})}));
describe('AssessmentResults',()=>{it('menampilkan hasil dan unduhan CSV',async()=>{render(<AssessmentResults assessmentId="asm-1"/>);expect(await screen.findByText('Budi')).toBeInTheDocument();expect(screen.getByText('7A')).toBeInTheDocument();expect(screen.getByText('8 / 10')).toBeInTheDocument();expect(screen.getByText(/perlu dinilai/i)).toBeInTheDocument();expect(screen.getByRole('link',{name:/csv/i})).toHaveAttribute('href','/v1/assessments/asm-1/results.csv')})});
